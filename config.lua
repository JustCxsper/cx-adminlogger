Config = {}

Config.Webhook = 'YOUR_DISCORD_WEBHOOK_URL'

Config.StaffAces = { 'admin', 'mod', 'support' }

-- Optional whitelist: if set, ONLY these commands will be logged (case-insensitive)
-- Leave empty {} to log anything passed to the export.
Config.LogCommands = {}
Config.Debug = true

